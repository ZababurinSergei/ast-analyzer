# ПРИМЕРНАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ РЕАЛИЗАЦИИ

## На основе графа зависимостей (100% уверенность)

---

## 1. МЕТОДОЛОГИЯ ОПРЕДЕЛЕНИЯ ПОСЛЕДОВАТЕЛЬНОСТИ

Анализ графа вызовов показывает **направленные зависимости**:

```json
"edges": [
  [0, 1, 1300, 0, 0],   // runCLI -> parseArgs
  [0, 2, 1300, 0, 0],   // runCLI -> findProjectRoot
  [0, 4, 1300, 0, 2],   // runCLI -> Logger
  [0, 8, 1320, 0, 1],   // runCLI -> loadConfig
  [0, 9, 1325, 0, 1],   // runCLI -> runAnalysis
  [1, 5, 500, 0, 0],    // parseArgs -> validateArgs
  [5, 6, 100, 0, 0],    // validateArgs -> normalizePath
  [6, 7, 200, 0, 0],    // normalizePath -> resolvePath
  [2, 7, 300, 0, 0],    // findProjectRoot -> resolvePath
  [9, 10, 400, 0, 0],   // runAnalysis -> buildProjectGraph
  [10, 11, 1267, 0, 2], // buildProjectGraph -> extractEntities
  [10, 12, 1267, 0, 2], // buildProjectGraph -> minifyCodeString
  [11, 12, 630, 0, 2],  // extractEntities -> minifyCodeString
  [0, 13, 1340, 0, 0],  // runCLI -> generateHTMLReport
  [13, 3, 27, 0, 0]     // generateHTMLReport -> escapeHtml
]
```

**Правила вывода последовательности:**
1. Строим топологическую сортировку графа
2. Функции, которые не вызывают другие (`leaves`), реализуются первыми
3. Функции, которые зависят от других, реализуются после них

---

## 2. УРОВНИ ЗАВИСИМОСТЕЙ (100% УВЕРЕННОСТЬ)

### Уровень 0 (Базовые утилиты) — реализуются первыми

| Функция | Файл | Почему |
|---------|------|--------|
| `escapeHtml` | `utils.ts` | Никого не вызывает, используется многими |
| `resolvePath` | `utils/path-utils.ts` | Никого не вызывает, базовый функционал |
| `Logger` | `utils/Logger.ts` | Никого не вызывает, базовый функционал |
| `getUserId` | `cli.ts` | Никого не вызывает |
| `getUserName` | `cli.ts` | Никого не вызывает |

**Порядок реализации:**
1. `Logger` — логирование нужно везде
2. `escapeHtml` — экранирование нужно для отчетов
3. `resolvePath` — работа с путями нужна всем
4. `getUserId`, `getUserName` — простые геттеры

---

### Уровень 1 (Базовые обработчики)

| Функция | Файл | Зависит от |
|---------|------|------------|
| `normalizePath` | `cli.ts` | `resolvePath` |
| `validateArgs` | `cli.ts` | `normalizePath` |
| `minifyCodeString` | `core/minifier.ts` | Никого не вызывает (leaf) |
| `extractEntities` | `core/entity-extractor.ts` | `minifyCodeString` |

**Порядок реализации:**
1. `minifyCodeString` — чистый функционал, ни от кого не зависит
2. `normalizePath` — зависит от `resolvePath`
3. `validateArgs` — зависит от `normalizePath`
4. `extractEntities` — зависит от `minifyCodeString`

---

### Уровень 2 (Парсеры и анализаторы)

| Функция | Файл | Зависит от |
|---------|------|------------|
| `parseArgs` | `cli.ts` | `validateArgs` |
| `findProjectRoot` | `cli.ts` | `resolvePath` |
| `generateHTMLReport` | `reporters/html-reporter.ts` | `escapeHtml` |

**Порядок реализации:**
1. `generateHTMLReport` — зависит от `escapeHtml` (уже есть)
2. `findProjectRoot` — зависит от `resolvePath` (уже есть)
3. `parseArgs` — зависит от `validateArgs` (уже есть)

---

### Уровень 3 (Ядро анализа)

| Функция | Файл | Зависит от |
|---------|------|------------|
| `buildProjectGraph` | `modes/project-graph.ts` | `extractEntities`, `minifyCodeString` |
| `loadConfig` | `config.ts` | Никого не вызывает (leaf) |

**Порядок реализации:**
1. `loadConfig` — чистый функционал
2. `buildProjectGraph` — зависит от `extractEntities` и `minifyCodeString`

---

### Уровень 4 (Бизнес-логика)

| Функция | Файл | Зависит от |
|---------|------|------------|
| `runAnalysis` | `modes/project-graph.ts` | `buildProjectGraph` |

**Порядок реализации:**
1. `runAnalysis` — зависит от `buildProjectGraph`

---

### Уровень 5 (Точка входа)

| Функция | Файл | Зависит от |
|---------|------|------------|
| `runCLI` | `cli.ts` | `parseArgs`, `findProjectRoot`, `loadConfig`, `runAnalysis`, `generateHTMLReport` |

**Порядок реализации:**
1. `runCLI` — последняя, зависит от всех остальных

---

## 3. ДЕТАЛЬНАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ РЕАЛИЗАЦИИ

### Этап 1: Базовые утилиты (5 функций)

```typescript
// 1.1. Logger.ts
class Logger {
  log(message: string): void { /* ... */ }
  info(message: string): void { /* ... */ }
  error(message: string): void { /* ... */ }
  warn(message: string): void { /* ... */ }
}

// 1.2. utils.ts
function escapeHtml(str: string): string {
  return str.replace(/[&<>"]/g, /* ... */);
}

// 1.3. utils/path-utils.ts
function resolvePath(path: string, base: string): string {
  // базовый функционал работы с путями
}

// 1.4. cli.ts (базовые геттеры)
function getUserId(): number { /* ... */ }
function getUserName(): string { /* ... */ }
```

---

### Этап 2: Базовые обработчики (4 функции)

```typescript
// 2.1. core/minifier.ts
function minifyCodeString(code: string, ast: AST): string {
  // чистый функционал минификации
}

// 2.2. cli.ts
function normalizePath(path: string): string {
  return resolvePath(path, process.cwd());
}

// 2.3. cli.ts
function validateArgs(args: any): boolean {
  const normalized = normalizePath(args.path);
  // валидация
}

// 2.4. core/entity-extractor.ts
function extractEntities(ast: AST, filePath: string): EntitiesResult {
  const minified = minifyCodeString(code, ast);
  // извлечение сущностей
}
```

---

### Этап 3: Парсеры и анализаторы (3 функции)

```typescript
// 3.1. reporters/html-reporter.ts
function generateHTMLReport(svg: string, dot: string, json: string): string {
  const escaped = escapeHtml(json);
  // генерация HTML
}

// 3.2. cli.ts
function findProjectRoot(startDir: string): string {
  return resolvePath(startDir, '..');
}

// 3.3. cli.ts
function parseArgs(): object {
  const args = process.argv.slice(2);
  if (!validateArgs(args)) {
    // ошибка
  }
  return parsed;
}
```

---

### Этап 4: Ядро анализа (2 функции)

```typescript
// 4.1. config.ts
function loadConfig(path: string): Config {
  // загрузка конфигурации
}

// 4.2. modes/project-graph.ts
function buildProjectGraph(entryPoint: string): Graph {
  const entities = extractEntities(ast, entryPoint);
  const minified = minifyCodeString(code, ast);
  // построение графа
}
```

---

### Этап 5: Бизнес-логика (1 функция)

```typescript
// 5.1. modes/project-graph.ts
async function runAnalysis(config: Config): Promise<AnalysisResult> {
  const graph = buildProjectGraph(config.entryPoint);
  // анализ графа
  return result;
}
```

---

### Этап 6: Точка входа (1 функция)

```typescript
// 6.1. cli.ts
async function runCLI(): Promise<void> {
  const args = parseArgs();
  const root = findProjectRoot(process.cwd());
  const config = loadConfig(root);
  const result = await runAnalysis(config);
  const html = generateHTMLReport(result.svg, result.dot, result.json);
  // вывод результата
}
```

---

## 4. ВИЗУАЛИЗАЦИЯ ПОСЛЕДОВАТЕЛЬНОСТИ

```
УРОВЕНЬ 0 (Базовые утилиты) — 1-й спринт (2 дня)
├── Logger
├── escapeHtml
├── resolvePath
├── getUserId
└── getUserName

УРОВЕНЬ 1 (Базовые обработчики) — 2-й спринт (2 дня)
├── minifyCodeString
├── normalizePath
├── validateArgs
└── extractEntities

УРОВЕНЬ 2 (Парсеры) — 3-й спринт (2 дня)
├── generateHTMLReport
├── findProjectRoot
└── parseArgs

УРОВЕНЬ 3 (Ядро) — 4-й спринт (2 дня)
├── loadConfig
└── buildProjectGraph

УРОВЕНЬ 4 (Бизнес-логика) — 5-й спринт (1 день)
└── runAnalysis

УРОВЕНЬ 5 (Точка входа) — 6-й спринт (1 день)
└── runCLI
```

**Итого: ~10 дней на реализацию всех функций**

---

## 5. ТАБЛИЦА ПО ФАЙЛАМ

| Файл | Функции | Уровень | Очередь |
|------|---------|---------|---------|
| `utils/Logger.ts` (f2) | `Logger` | 0 | 1 |
| `utils.ts` (f28) | `escapeHtml`, `showHelp` | 0, 2 | 1 |
| `utils/path-utils.ts` (f29) | `resolvePath` | 0 | 1 |
| `core/minifier.ts` (f3) | `minifyCodeString` | 1 | 2 |
| `core/entity-extractor.ts` (f7) | `extractEntities` | 1 | 2 |
| `reporters/html-reporter.ts` (f21) | `generateHTMLReport` | 2 | 3 |
| `config.ts` (f23) | `loadConfig` | 3 | 4 |
| `modes/project-graph.ts` (f8) | `buildProjectGraph`, `runAnalysis` | 3, 4 | 4, 5 |
| `cli.ts` (f1) | `getUserId`, `getUserName`, `normalizePath`, `validateArgs`, `findProjectRoot`, `parseArgs`, `runCLI` | 0, 1, 2, 5 | 1, 2, 3, 6 |

---

## 6. КРИТИЧЕСКИЙ ПУТЬ (ИЗМЕНЕНИЯ ВЛИЯЮТ НА ВСЕ)

```
runCLI
  ├── parseArgs
  │   └── validateArgs
  │       └── normalizePath
  │           └── resolvePath ◄─── БАЗОВАЯ ЗАВИСИМОСТЬ
  ├── findProjectRoot
  │   └── resolvePath ◄─── БАЗОВАЯ ЗАВИСИМОСТЬ
  ├── loadConfig
  ├── runAnalysis
  │   └── buildProjectGraph
  │       ├── extractEntities
  │       │   └── minifyCodeString ◄─── БАЗОВАЯ ЗАВИСИМОСТЬ
  │       └── minifyCodeString ◄─── БАЗОВАЯ ЗАВИСИМОСТЬ
  └── generateHTMLReport
      └── escapeHtml ◄─── БАЗОВАЯ ЗАВИСИМОСТЬ
```

**Критические базовые функции (должны быть реализованы первыми):**
1. `resolvePath` — используется в 3 функциях
2. `Logger` — используется в 4 функциях
3. `escapeHtml` — используется в `generateHTMLReport`
4. `minifyCodeString` — используется в 2 функциях

---

## 7. РИСКИ ПРИ РЕАЛИЗАЦИИ

| Риск | Функция | Решение |
|------|---------|---------|
| Циклическая зависимость | `runCLI` ↔ `parseArgs` | Реализовать как отдельные модули |
| Сложная логика | `buildProjectGraph` | Разбить на подфункции |
| Внешние зависимости | `loadConfig` | Использовать моки на этапе тестирования |
| Асинхронность | `runCLI`, `runAnalysis`, `loadConfig` | Использовать async/await с начала |

---

## 8. РЕКОМЕНДУЕМЫЙ ПЛАН ПО СПРИНТАМ

| Спринт | Дни | Функции | Результат |
|--------|-----|---------|-----------|
| **Sprint 1** | 2 | `Logger`, `escapeHtml`, `resolvePath`, `getUserId`, `getUserName` | Базовые утилиты готовы |
| **Sprint 2** | 2 | `minifyCodeString`, `normalizePath`, `validateArgs`, `extractEntities` | Базовые обработчики готовы |
| **Sprint 3** | 2 | `generateHTMLReport`, `findProjectRoot`, `parseArgs` | Парсеры готовы |
| **Sprint 4** | 2 | `loadConfig`, `buildProjectGraph` | Ядро готово |
| **Sprint 5** | 1 | `runAnalysis` | Бизнес-логика готова |
| **Sprint 6** | 1 | `runCLI` | Точка входа готова |

**Итого: 10 дней**



