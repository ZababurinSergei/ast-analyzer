import { create } from 'storybook/theming';
import InfoenergoLogo from '../public/logos/InfoenergoLogo.svg';

export default create({
    base: 'light',
    brandTitle: 'Инфоэнерго',
    brandImage: InfoenergoLogo,
});
