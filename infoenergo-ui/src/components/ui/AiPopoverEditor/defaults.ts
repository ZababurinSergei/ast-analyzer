import type { EditorDefaultConfig } from '@/interfaces/common.ts';

/** Пропсы для редактора по умолчанию */
export const EDITOR_DEFAULT_CONFIG: EditorDefaultConfig = {
    width: '220px',
    height: '350px',
    selectEvent: 'row-click',
};
